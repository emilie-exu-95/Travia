<?php

/*
 * TODO : HANDLE RESET PASSWORD AFTER FORM SUBMISSION
 *
 * Verify if new password fulfills all criteria
 * Verify that new password hasn't already been used the last three times
 * Update new password to current password for the user
 * Add new password to the password table, delete all user password except the last three (keep the minimum)
 * Add new log, password change for user
 * Send email that the email has been successfully changed
 *
 */